{
  "permissions": {
    "allow": [
      "Bash(git add:*)",
      "Bash(git commit -m \"$\\(cat <<''EOF''\nfeat: use GH_CONFIG_DIR for per-repo gh config in yuvod\n\nReplace `gh auth switch` with a local GH_CONFIG_DIR so yuvod\nrepos use a separate gh configuration directory automatically.\nEOF\n\\)\")",
      "Bash(gh api:*)",
      "Bash(gh repo:*)"
    ]
  }
}
